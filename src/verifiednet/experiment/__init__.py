"""Controlled experiments (Gate 15).

The TOP composition layer for model-quality experiments: it composes the
verified training lifecycle (Gate 10) and the deterministic measurement
stack (Gates 7/9/12/13) into ONE preregistered, one-run, one-checkpoint
controlled experiment. Nothing imports this package; it never appears in the
deterministic truth chain, never trains or evaluates anything itself
(execution and inference stay behind their existing gates), and it is
ML-import-free — it binds identities and classifies outcomes from raw
counts under a frozen success policy (ADR-0033).
"""

from verifiednet.experiment.analysis import (
    EXPERIMENT_OUTCOMES,
    ExperimentOutcome,
    ExperimentPrimaryMetrics,
    FamilyPairedCounts,
    TestFirewallAudit,
    audit_test_firewall,
    cap_training_corpus,
    classify_experiment_outcome,
    compute_family_paired_counts,
    compute_partition_paired_counts,
    corpus_distributions,
    derive_firewall_audit_id,
    extract_primary_metrics,
    success_policy_checks,
)
from verifiednet.experiment.spec import (
    EXPERIMENT_PHASE_SEQUENCE,
    GATE15_FAILURE_CRITERIA,
    GATE15_PRIMARY_METRICS,
    GATE15_SECONDARY_METRICS,
    GATE15_SUCCESS_CRITERIA,
    ControlledExperimentError,
    ControlledTrainingExperimentSpec,
    ExperimentPhase,
    ExperimentPhaseLog,
    ExperimentRuntimeEnvelope,
    ExperimentSuccessPolicy,
    advance_phase,
    build_experiment_spec,
    build_success_policy,
    derive_experiment_id,
    derive_success_policy_id,
    start_phase_log,
)
from verifiednet.experiment.store import (
    EXPERIMENT_CONTENT_FILES,
    BenchmarkBinding,
    BenchmarkRankingRow,
    CheckpointBinding,
    ControlledExperimentManifest,
    ControlledTrainingExperimentResult,
    EvaluationBindings,
    ExperimentVerificationResult,
    LoadedControlledExperiment,
    PairedSummary,
    ReliabilitySummary,
    TrainingPhaseBinding,
    WrittenControlledExperiment,
    WrittenPreregistration,
    build_experiment_result,
    compute_experiment_result_digest,
    derive_experiment_result_id,
    preregister_experiment,
    read_controlled_experiment,
    verify_controlled_experiment,
    write_experiment_result,
)

__all__ = [
    "EXPERIMENT_CONTENT_FILES",
    "EXPERIMENT_OUTCOMES",
    "EXPERIMENT_PHASE_SEQUENCE",
    "GATE15_FAILURE_CRITERIA",
    "GATE15_PRIMARY_METRICS",
    "GATE15_SECONDARY_METRICS",
    "GATE15_SUCCESS_CRITERIA",
    "BenchmarkBinding",
    "BenchmarkRankingRow",
    "CheckpointBinding",
    "ControlledExperimentError",
    "ControlledExperimentManifest",
    "ControlledTrainingExperimentResult",
    "ControlledTrainingExperimentSpec",
    "EvaluationBindings",
    "ExperimentOutcome",
    "ExperimentPhase",
    "ExperimentPhaseLog",
    "ExperimentPrimaryMetrics",
    "ExperimentRuntimeEnvelope",
    "ExperimentSuccessPolicy",
    "ExperimentVerificationResult",
    "FamilyPairedCounts",
    "LoadedControlledExperiment",
    "PairedSummary",
    "ReliabilitySummary",
    "TestFirewallAudit",
    "TrainingPhaseBinding",
    "WrittenControlledExperiment",
    "WrittenPreregistration",
    "advance_phase",
    "audit_test_firewall",
    "build_experiment_result",
    "build_experiment_spec",
    "build_success_policy",
    "cap_training_corpus",
    "classify_experiment_outcome",
    "compute_experiment_result_digest",
    "compute_family_paired_counts",
    "compute_partition_paired_counts",
    "corpus_distributions",
    "derive_experiment_id",
    "derive_experiment_result_id",
    "derive_firewall_audit_id",
    "derive_success_policy_id",
    "extract_primary_metrics",
    "preregister_experiment",
    "read_controlled_experiment",
    "start_phase_log",
    "success_policy_checks",
    "verify_controlled_experiment",
    "write_experiment_result",
]
